[
  {
    "id": "POL-PRODUCTION-SCOPE",
    "kind": "forbidden_path",
    "description": "Production infrastructure changes require a separate human-owned workflow.",
    "paths": ["infrastructure/production/**"]
  },
  {
    "id": "POL-SECRETS",
    "kind": "forbidden_path",
    "description": "Secret-bearing environment files are outside this capability.",
    "paths": [".env", ".env.*", "secrets/**"]
  },
  {
    "id": "POL-TESTS",
    "kind": "required_validator",
    "description": "Configuration changes require the approved unit test validator.",
    "required_validator": "python-unit-tests"
  },
  {
    "id": "POL-FILES",
    "kind": "required_validator",
    "description": "Every declared change file must exist inside the governed project.",
    "required_validator": "requested-files-exist"
  },
  {
    "id": "POL-PERSISTENCE-OWNERSHIP",
    "kind": "persistence_ownership",
    "description": "A logical configuration value must have one explicit source of truth.",
    "trigger_effect": "introduces-persistence-source",
    "existing_source": "src/config/service.py::CONFIG_STORE",
    "options": [
      "preserve the existing source of truth",
      "migrate ownership with an explicit migration plan",
      "define and govern synchronization semantics"
    ]
  }
]
