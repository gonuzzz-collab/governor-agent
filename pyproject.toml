[project]
name = "governor-agent"
version = "0.1.0"
description = "Govern AI-assisted software changes through explicit authority, policies, permits, validators, evidence, and human escalation."
requires-python = ">=3.12"
dependencies = []

[tool.ruff]
line-length = 100
target-version = "py312"
