#!/usr/bin/env bash
set -euo pipefail

if (( $# != 0 )); then
  echo "Uso: scripts/test" >&2
  exit 2
fi

PROJECT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"
PYTHONPATH="${PROJECT_DIR}/src" python3 -m unittest discover -s "${PROJECT_DIR}/tests"
